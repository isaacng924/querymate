# Required for the LLM writer/critic (the executor + eval comparison run without it).
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides (defaults live in querymate/settings.py):
# QUERYMATE_WRITER_MODEL=claude-sonnet-4-6
# QUERYMATE_ESCALATE_MODEL=claude-opus-4-8
# QUERYMATE_MAX_ATTEMPTS=3
# QUERYMATE_STATEMENT_TIMEOUT_S=10

# Optional Langfuse (Phase 3):
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
